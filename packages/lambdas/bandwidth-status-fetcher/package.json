{
  "name": "bandwidth-status-fetcher-lambda",
  "version": "0.1.0",
  "description": "fetches bandwidth status data",
  "main": "index.js",
  "scripts": {
    "test": "APP_ENV=local NODE_ENV=test jest",
    "deploy": "pwd"
  },
  "author": "",
  "license": "UNLICENSED",
  "private": true,
  "deploy": {
    "serviceName": "bandwidth-status-fetcher-lambda",
    "type": "lambda",
    "awsParameter": "bandwidthStatusFetcherLambdaTag"
  },
  "dependencies": {
    "fetchers-configuration": "^0.0.2"
  }
}
