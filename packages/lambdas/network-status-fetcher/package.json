{
  "name": "@test/network-status-fetcher-lambda",
  "version": "0.1.1",
  "description": "fetches network status data",
  "main": "index.js",
  "scripts": {
    "test": "APP_ENV=local NODE_ENV=test jest",
    "deploy": "pwd"
  },
  "author": "",
  "license": "UNLICENSED",
  "private": true,
  "deploy": {
    "serviceName": "network-status-fetcher-lambda",
    "type": "lambda",
    "awsParameter": "networkStatusFetcherLambdaTag"
  },
  "dependencies": {
    "fetchers-configuration": "^0.0.3"
  }
}
